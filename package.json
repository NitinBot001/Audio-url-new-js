{
  "name": "playwright-audio-downloader",
  "version": "1.0.0",
  "description": "An Express server to fetch and download YouTube audio using Playwright and Axios",
  "main": "server.js",
  "scripts": {
  "postinstall": "npx playwright install-deps && npx playwright install",
  "test": "node server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "playwright": "^1.41.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
